[package]
name = "flashback"
version = "1.18.0"
description = "Captura y edición ligera de clips de juego para Windows"
authors = ["Josh Bernal"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "flashback_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["protocol-asset", "tray-icon"] }
tauri-plugin-opener = "2"
tauri-plugin-global-shortcut = "2"
tauri-plugin-single-instance = "2"
tauri-plugin-updater = "2"
tauri-plugin-process = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
# Solo carátulas/detectables cacheados, nunca en el camino de captura. TLS nativo (schannel)
# vía native-tls y sin http2/charset para no arrastrar el stack por defecto de reqwest.
reqwest = { version = "0.12", default-features = false, features = ["json", "native-tls"] }
base64 = "0.22"
tokio = { version = "1", features = ["rt", "sync"] }
urlencoding = "2"
discord-rich-presence = "0.2"
# time 0.3.48 rompe la coherencia con cookie 0.18.1 (que arrastra Tauri): error E0119.
time = "=0.3.47"

# Prioridad #1 del proyecto: binario ligero y rápido. panic="unwind" (default) a propósito:
# el pipeline de captura contiene los panics con catch_unwind para auto-recuperarse (§4.4).
[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true

[target.'cfg(windows)'.dependencies]
# Ya viene con `windows`; se nombra para las transformaciones de Direct2D del compositor.
windows-numerics = "0.2"
windows = { version = "0.61", features = [
    "Win32_Foundation",
    "Win32_UI_WindowsAndMessaging",
    "Win32_UI_Shell",
    "Win32_System_Diagnostics_ToolHelp",
    "Win32_System_Threading",
    "Win32_System_Com",
    "Win32_System_Com_StructuredStorage",
    "Win32_System_Ole",
    "Win32_System_Variant",
    "Win32_System_SystemInformation",
    "Foundation",
    "Devices_Enumeration",
    "Graphics",
    "Win32_Media_Audio",
    "Win32_Security",
    "Graphics_Capture",
    "Graphics_DirectX",
    "Graphics_DirectX_Direct3D11",
    "Win32_Graphics_Direct3D",
    "Win32_Graphics_Direct3D11",
    "Win32_Graphics_Dxgi",
    "Win32_Graphics_Dxgi_Common",
    "Win32_Graphics_Gdi",
    "Win32_Graphics_Imaging",
    "Win32_Graphics_Direct2D",
    "Win32_Graphics_Direct2D_Common",
    "Win32_Graphics_DirectComposition",
    "Win32_Graphics_DirectWrite",
    "Win32_UI_HiDpi",
    "Win32_Media_MediaFoundation",
    "Win32_System_WinRT_Direct3D11",
    "Win32_System_WinRT_Graphics_Capture",
] }

